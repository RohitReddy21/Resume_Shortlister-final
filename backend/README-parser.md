Parser implementation added under `app/services/parser`.

To install optional parser dependencies:

```bash
python -m pip install -r backend/requirements-parser.txt
```

Run sample parser:

```bash
python backend/scripts/parse_sample.py path/to/resume.pdf
```
