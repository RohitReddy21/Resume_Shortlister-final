services:
  - type: web
    name: resumeparser-api
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: .
    plan: starter
    healthCheckPath: /health
    initialDeployHook: python scripts/seed_admin.py
    envVars:
      - key: APP_ENV
        value: production
      - key: API_V1_PREFIX
        value: /api/v1
      - key: DATABASE_URL
        fromDatabase:
          name: resumeparser-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: FRONTEND_URL
        sync: false
      - key: FRONTEND_HOSTNAME
        sync: false
      - key: UPLOAD_ROOT
        value: /data/uploads
      - key: ADMIN_EMAIL
        value: admin@example.com
      - key: ADMIN_PASSWORD
        sync: false
      - key: ATS_SHORTLIST_THRESHOLD
        value: "70"
      - key: RESUME_PARSER_USE_CELERY
        value: "false"
    disk:
      name: resume-uploads
      mountPath: /data
      sizeGB: 1

databases:
  - name: resumeparser-db
    databaseName: resumeparser
    user: resumeparser
